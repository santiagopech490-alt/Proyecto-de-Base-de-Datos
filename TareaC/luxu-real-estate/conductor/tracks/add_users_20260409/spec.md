# Track Specification: User Management - Add Users

## Objective
Implement a functional interface to add new users to the system, persisting them in the Supabase `profiles` table.

## Requirements
- User input form for adding users (Name, Email, Role).
- Server action to insert user profile into Supabase.
- Form validation using Zod.
- Immediate UI feedback after submission.
